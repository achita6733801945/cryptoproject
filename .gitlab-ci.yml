pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir -p public
    - cp *.html public/ || true
    - cp *.css public/ || true
    - cp *.js public/ || true
  artifacts:
    paths:
      - public
  only:
    - main