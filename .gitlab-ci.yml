pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp *.css public/ || true
    - cp *.js public/ || true
  artifacts:
    paths:
      - public
  only:
    - main